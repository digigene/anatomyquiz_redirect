name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate config.js
        run: |
          node <<'NODE'
          const fs = require("fs");
          const config = {
            apiBaseUrl: "https://r2fihr72e5.execute-api.ap-southeast-2.amazonaws.com/prod",
          };
          fs.writeFileSync("config.js", `window.AQ_CONFIG = ${JSON.stringify(config)};`);
          NODE

      - name: Sync 404.html
        run: cp index.html 404.html

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
